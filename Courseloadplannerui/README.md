
  # Course Load Planner UI

  This is a code bundle for Course Load Planner UI. The original project is available at https://www.figma.com/design/Lc2LTAfnRm6VEiuheDSqDo/Course-Load-Planner-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  